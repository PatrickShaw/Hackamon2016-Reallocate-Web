import React from 'react';
const BasicLayout = props => (
    <html>
        <head>

        </head>
        <body>
            {props.children}
        </body>
    </html>
);
export {BasicLayout};
export default BasicLayout;